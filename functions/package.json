{
  "name": "pixel-place-functions",
  "version": "1.0.0",
  "private": true,
  "license": "CC-BY-NC-ND-4.0",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.5.0",
    "jsonwebtoken": "^9.0.3",
    "nodemailer": "^8.0.7",
    "stripe": "^14.21.0"
  },
  "devDependencies": {
    "@types/nodemailer": "^8.0.0",
    "typescript": "^5.3.0"
  }
}
