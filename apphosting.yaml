scripts:
  # Static export build; avoids framework adapter assumptions about SSR output.
  buildCommand: npm run build
  runCommand: node apphosting-static-server.mjs

outputFiles:
  serverApp:
    include:
      - out
      - apphosting-static-server.mjs
