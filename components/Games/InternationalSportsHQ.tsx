import React from 'react'
import SportTemplate from './SportTemplate'
import { getSport } from './sports-config'

export default function Tennis() {
  const meta = getSport('tennis')!
  return <SportTemplate meta={meta} />
}import React from 'react'
import SportTemplate from './SportTemplate'
import { getSport } from './sports-config'

export default function Pickleball() {
  const meta = getSport('pickleball')!
  return <SportTemplate meta={meta} />
}import React from 'react'
import SportTemplate from './SportTemplate'
import { getSport } from './sports-config'

export default function Basketball() {
  const meta = getSport('basketball')!
  return <SportTemplate meta={meta} />
}import React from 'react'
import SportTemplate from './SportTemplate'
import { getSport } from './sports-config'

export default function Baseball() {
  const meta = getSport('baseball')!
  return <SportTemplate meta={meta} />
}import React from 'react'
import SportTemplate from './SportTemplate'
import { getSport } from './sports-config'

export default function Swimming() {
  const meta = getSport('swimming')!
  return <SportTemplate meta={meta} />
}import React from 'react'
import SportTemplate from './SportTemplate'
import { getSport } from './sports-config'

export default function Lacrosse() {
  const meta = getSport('lacrosse')!
  return <SportTemplate meta={meta} />
}import React from 'react'
import SportTemplate from './SportTemplate'
import { getSport } from './sports-config'

export default function FieldHockey() {
  const meta = getSport('field-hockey') || getSport('fieldhockey')
  return <SportTemplate meta={meta!} />
}import React from 'react'
import SportTemplate from './SportTemplate'
import { getSport } from './sports-config'

export default function IceHockey() {
  const meta = getSport('ice-hockey') || getSport('icehockey')
  return <SportTemplate meta={meta!} />
}import React from 'react'
import SportTemplate from './SportTemplate'
import { getSport } from './sports-config'

export default function Cricket() {
  const meta = getSport('cricket')!
  return <SportTemplate meta={meta} />
}import React from 'react'
import SportTemplate from './SportTemplate'
import { getSport } from './sports-config'

export default function Soccer() {
  const meta = getSport('soccer')!
  return <SportTemplate meta={meta} />
}import React from 'react'
import SportTemplate from './SportTemplate'
import { getSport } from './sports-config'

export default function AmericanFootball() {
  const meta = getSport('american-football') || getSport('americanfootball')
  return <SportTemplate meta={meta!} />
}import React from 'react'
import SportTemplate from './SportTemplate'
import { getSport } from './sports-config'

export default function Handball() {
  const meta = getSport('handball')!
  return <SportTemplate meta={meta} />
}import React from 'react'
import SportTemplate from './SportTemplate'
import { getSport } from './sports-config'

export default function Gymnastics() {
  const meta = getSport('gymnastics')!
  return <SportTemplate meta={meta} />
}import React from 'react'
import SportTemplate from './SportTemplate'
import { getSport } from './sports-config'

export default function Volleyball() {
  const meta = getSport('volleyball')!
  return <SportTemplate meta={meta} />
}import React from 'react'
import SportTemplate from './SportTemplate'
import { getSport } from './sports-config'

export default function Golf() {
  const meta = getSport('golf')!
  return <SportTemplate meta={meta} />
}import React from 'react'
import SportTemplate from './SportTemplate'
import { getSport } from './sports-config'

export default function HorsebackRiding() {
  const meta = getSport('horseback-riding') || getSport('horsebackriding')
  return <SportTemplate meta={meta!} />
}import React from 'react'
import SportTemplate from './SportTemplate'
import { getSport } from './sports-config'

export default function Polo() {
  const meta = getSport('polo')!
  return <SportTemplate meta={meta} />
}import React from 'react'
import SportTemplate from './SportTemplate'
import { getSport } from './sports-config'

export default function WaterPolo() {
  const meta = getSport('water-polo') || getSport('waterpolo')
  return <SportTemplate meta={meta!} />
}import React from 'react'
import SportTemplate from './SportTemplate'
import { getSport } from './sports-config'

export default function Archery() {
  const meta = getSport('archery')!
  return <SportTemplate meta={meta} />
}import React from 'react'
import SportTemplate from './SportTemplate'
import { getSport } from './sports-config'

export default function Boxing() {
  const meta = getSport('boxing')!
  return <SportTemplate meta={meta} />
}import React from 'react'
import SportTemplate from './SportTemplate'
import { getSport } from './sports-config'

export default function Wrestling() {
  const meta = getSport('wrestling')!
  return <SportTemplate meta={meta} />
}import React from 'react'
import SportTemplate from './SportTemplate'
import { getSport } from './sports-config'

export default function Rugby() {
  const meta = getSport('rugby')!
  return <SportTemplate meta={meta} />
}import React from 'react'
import SportTemplate from './SportTemplate'
import { getSport } from './sports-config'

export default function Dodgeball() {
  const meta = getSport('dodgeball')!
  return <SportTemplate meta={meta} />
}
