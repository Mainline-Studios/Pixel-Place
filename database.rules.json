{
  "rules": {
    ".read": false,
    ".write": false,
    "open_world": {
      "$room": {
        ".read": true,
        "players": {
          "$playerId": {
            ".write": true,
            ".validate": "!newData.exists() || (newData.hasChildren(['username', 'x', 'y', 'z', 'rotY', 'anim', 'updatedAt']) && newData.child('username').isString() && newData.child('x').isNumber() && newData.child('y').isNumber() && newData.child('z').isNumber() && newData.child('rotY').isNumber() && newData.child('anim').isString() && newData.child('updatedAt').isNumber())"
          }
        },
        "chat": {
          ".write": "!newData.exists()",
          "$msgId": {
            ".write": "!newData.exists() || (!data.exists() && newData.hasChildren(['username', 'text', 'createdAt']) && newData.child('username').isString() && newData.child('text').isString() && newData.child('text').val().length > 0 && newData.child('text').val().length <= 200 && newData.child('createdAt').isNumber())",
            ".validate": "!newData.exists() || newData.hasChildren(['username', 'text', 'createdAt'])"
          }
        },
        "mod": {
          "chatClearAt": {
            ".write": true,
            ".validate": "newData.isNumber()"
          }
        },
        "kicks": {
          "$playerId": {
            ".write": true,
            ".validate": "!newData.exists() || (newData.hasChildren(['username', 'by', 'at', 'until']) && newData.child('username').isString() && newData.child('by').isString() && newData.child('at').isNumber() && newData.child('until').isNumber())"
          }
        }
      }
    },
    "open_world_chat_everywhere": {
      ".read": true,
      ".write": "!newData.exists()",
      "$msgId": {
        ".write": "!newData.exists() || (!data.exists() && newData.hasChildren(['username', 'text', 'createdAt']) && newData.child('username').isString() && newData.child('text').isString() && newData.child('text').val().length > 0 && newData.child('text').val().length <= 200 && newData.child('createdAt').isNumber())",
        ".validate": "!newData.exists() || newData.hasChildren(['username', 'text', 'createdAt'])"
      }
    },
    "open_world_chat_everywhere_meta": {
      ".read": true,
      "clearAt": {
        ".write": true,
        ".validate": "newData.isNumber()"
      }
    },
    "open_world_servers": {
      ".read": true,
      "global": {
        "$slot": {
          ".write": true,
          ".validate": "!newData.exists() || (newData.hasChildren(['playerCount', 'updatedAt']) && newData.child('playerCount').isNumber() && newData.child('updatedAt').isNumber())"
        }
      },
      "private": {
        "$code": {
          ".write": true,
          ".validate": "!newData.exists() || (newData.hasChildren(['code', 'roomId', 'host', 'createdAt', 'expiresAt']) && newData.child('code').isString() && newData.child('roomId').isString() && newData.child('host').isString() && newData.child('createdAt').isNumber() && newData.child('expiresAt').isNumber())"
        }
      }
    }
  }
}
