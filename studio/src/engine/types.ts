/**
 * Shared engine-level type aliases used across components and the editor UI.
 */

/** Built-in primitive shapes the editor can instantiate without external assets. */
export type PrimitiveKind = "box" | "sphere" | "cylinder" | "plane";
